# Archived Verification Report: Supervisor and Agent Pool

**Status:** Passed

**Target features:** FEAT-022 through FEAT-026

**Evaluated at:** 2026-08-25T05:11:00+08:00

## Deterministic evidence

- Vitest: 17/17 files and 54/54 assertions.
- Python Runtime: 27/27 tests.
- Fixture generation check, TypeScript typecheck, Vite 8.2.2 production build, npm audit with zero findings, gateway smoke, WSL/GPU preflight, and whitespace validation all exited zero.
- Gateway verified one `local-supervisor`, shared final-text/STT orchestration, task routing, deterministic `BLOCKED_POLICY`, and protocol rejection.

## Live Supervisor evidence

Pinned `Qwen/Qwen3-1.7B` revision `70d244cc86ccca08cf5af4e1e306ecf908b1ad5e`:

- Exported official-source NF4 artifact: 24/24 schema-valid and 24/24 route-correct; 757.040 ms p50; 1,288 MiB process allocation; board 3,858 to 5,345 MiB.
- BF16 comparison: 24/24 schema-valid and 23/24 route-correct; 623.158 ms p50; 3,282 MiB process allocation. The missed exact command is intercepted by the deterministic command layer.
- Supervisor-only WebSocket service smoke emitted the expected manifest, task, response, and state sequence.

## Capacity and safety result

ASR/Supervisor co-residency failed across independent-process, CPU-offload, and single-process arrangements because of GPU allocator behavior or Windows/WSL commit exhaustion. The accepted deployment uses mutually exclusive `voice` and `supervisor` profiles and refuses the unsafe pair. A post-experiment live ASR smoke passed with 15 partials, a non-empty final, 7,132 MiB loaded board use, and 5,150 MiB recovery headroom.

Hermes deployment remained disabled because the installed one-shot CLI loads tools while auto-bypassing approvals. Its bounded injected adapter passed deterministic timeout/retry/budget/circuit-breaker tests but requires an audited read-only transport before enablement.

## Decisions

- FEAT-022 through FEAT-026: `passes: true`.
