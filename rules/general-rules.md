# General Coding Rules

- Use clear, declarative naming for functions and variables.
- Maintain single responsibility per module.
- Always handle errors explicitly at boundary interfaces.
- Avoid hidden state mutations; prefer immutable data transformations.
