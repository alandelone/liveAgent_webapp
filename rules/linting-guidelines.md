# Linting & Formatting Guidelines

- Ensure code passes static analysis with zero errors before passing stage gates.
- Follow 2-space indentation and consistent naming conventions.
- Auto-format code using the repository's formatter (`npm run format`).
